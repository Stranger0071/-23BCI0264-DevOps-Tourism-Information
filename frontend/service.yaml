apiVersion: v1
kind: Service
metadata:
  name: tourism-frontend-service
  labels:
    app: tourism-frontend
spec:
  type: LoadBalancer
  selector:
    app: tourism-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
