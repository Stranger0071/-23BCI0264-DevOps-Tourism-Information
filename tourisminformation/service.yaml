apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: tourisminformation
spec:
  type: ClusterIP
  selector:
    app: tourisminformation
  ports:
    - name: http
      port: 8081
      targetPort: 8081
